#!/usr/bin/env bash
# SOURCE before a deliberate fresh reservation. Keeps TOKEN; changes no server data.
# Old server records remain available for diagnosis; this does not cancel them.
unset UPLOAD_URL ID VIDEO MEDIA BYTES
